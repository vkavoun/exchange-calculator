import template from './converter.html';
import controller from './converter.controller';
import './converter.scss';

let currencyConverterComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default currencyConverterComponent;
